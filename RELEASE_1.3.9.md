# 1.3.9

## Fixes

- Fixed migration of the built-in Facebook message template.
- Existing installations that still use the previous built-in default now receive the new classification placeholder automatically.
- User-customized Facebook templates are preserved unchanged.
- The default Facebook message can now show the incident classification, for example `Buitenbrand` or `Woningbrand`.
